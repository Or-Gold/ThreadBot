import { CommandInteraction, ThreadChannel } from "discord.js";

export default async function issueGithub(interaction: CommandInteraction, thread: ThreadChannel) {
    interaction.reply("Not implemented!") 
}
